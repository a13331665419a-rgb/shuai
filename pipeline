# 技能流水线编排器（Pipeline Orchestrator）

本文件定义如何组合使用本仓库中的三个核心技能，复现完整的"AI北美爆款短剧"AIGC生产流程。

---

## 技能清单

| 技能文件 | 技能名称 | 输入 | 输出 |
|---------|---------|------|------|
| `one` | 视觉词典生成器 | 完整剧本 | 视觉词典（角色/场景/物件/风格/情绪地图） |
| `daoyan` | 导演版画面剧本生成器 | 完整剧本 | 导演版本画面剧本（逐集） |
| `two` | 视听单元生成器 | 完整剧本 + `one` 的输出 | 视听单元列表（可直接喂给AI视频工具） |

---

## 标准使用流程

### 触发指令格式

```
加载技能: daoyan+one+two
【完整剧本】
{{粘贴完整剧本}}
```

### 执行顺序

```
第一步（并行）：
  ├─ one   → 生成视觉词典
  └─ daoyan → 生成导演版画面剧本

第二步（在 one 完成后执行）：
  └─ two（剧本 + one 的视觉词典输出）→ 生成视听单元
```

> **为什么 `two` 必须在 `one` 之后？**
> `two` 的每个视听单元都需要把视觉词典中的角色外貌锁定、场景词条、风格声明内嵌进去，
> 以保证 AI 视频工具能独立理解每个单元。缺少视觉词典，`two` 会生成出角色描述不一致的单元。

---

## 分步手动使用方式（推荐用于跨会话操作）

如果你需要跨多次对话使用，请按以下步骤操作：

### 第1轮对话 — 生成视觉词典

复制 `one` 文件的全部内容，将末尾 `{{粘贴完整剧本}}` 替换为你的完整剧本，发送给 AI。

保存输出结果（标注为"视觉词典"）。

### 第2轮对话 — 生成导演版画面剧本

复制 `daoyan` 文件的全部内容，将末尾三处输入区替换：

```
【完整剧本】
{{粘贴完整剧本}}          ← 替换为你的剧本

（可选）【第一阶段视觉词典】
{{粘贴第一阶段完整输出}}   ← 替换为第1轮的视觉词典（可选，但建议加入以提升一致性）
```

保存输出结果（标注为"导演版画面剧本"）。

### 第3轮对话 — 生成视听单元

复制 `two` 文件的全部内容，将末尾输入区替换：

```
【完整剧本】
{{粘贴完整剧本}}          ← 替换为你的剧本

【第一阶段视觉词典】
{{粘贴第一阶段完整输出}}   ← 替换为第1轮输出的视觉词典（必填）

（可选）【生成范围】
{{如"请生成Unit-01至Unit-05"}}
```

---

## 单技能独立使用

你也可以只使用其中一个技能：

- **只需要视觉词典** → 单独使用 `one`
- **只需要导演版剧本** → 单独使用 `daoyan`
- **已有视觉词典，只需要视听单元** → 单独使用 `two`

---

## 各技能所在文件

```
shuai/
├── one       ← 技能1：视觉词典生成器
├── daoyan    ← 技能2：导演版画面剧本生成器
├── two       ← 技能3：视听单元生成器
└── pipeline  ← 本文件：流程编排说明
```

---

## 输出质量检查

完成三个技能后，建议对照以下要点自查：

| 检查项 | 来源技能 |
|--------|---------|
| 每个角色是否有完整外貌锁定（至少7个可见细节） | `one` |
| 每个场景是否有空间锁定 + 光照状态 | `one` |
| 关键物件是否有外观锁定 + 叙事功能 | `one` |
| 每一集是否有导演目标 + 核心卖点 + 结尾卡点 | `daoyan` |
| 每个视听单元是否以风格声明起头 | `two` |
| 角色首次出现是否内嵌外貌锁定 + 服装 | `two` |
| 相邻单元出帧/入帧是否一致 | `two` |
